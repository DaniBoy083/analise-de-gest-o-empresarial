[build]
  command = "python -m pip install --upgrade pip && python -m pip install -r requirements.txt && npm install && npm run build:dashboard && python main.py --output-dir docs/site"
  publish = "docs/site"

[build.environment]
  PYTHON_VERSION = "3.12"
